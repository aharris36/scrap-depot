@model List<Scrap>
    <ul>
        @foreach (var scrap in Model)
        {
            <li><a href="/Scrap/Details/@scrap.Id">@scrap.Name</a> — @scrap.Depositied deposited.</li>
        }
    </ul>
</div>
<script src="https://jgrissom.github.io/dotnet-web-dev/week-04/homework-checks.js"></script>